package com.example.shareadperfence_pifort;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.ArrayAdapter;

import java.sql.Array;
import java.util.ArrayList;

public class ShowRecods extends AppCompatActivity  {

    @Override
    protected void onCreate (Bundle savedInstanceState) {
        ArrayAdapter adapter;
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_show_recods);



       // ArrayAdapter<User> ad = new ArrayAdapter<User>(this,MyDatabase.class,adapter.addAll ( databaseList ().getClass ().toString ());
        //startActivities ( databaseList (ad.addAll (  )));

    }

    public void Showoutput(){
        
    }

    }

